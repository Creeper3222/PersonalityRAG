[project]
name = "personalityrag"
version = "0.1.2"
description = "Independent, LivingMemory-compatible personality memory RAG service"
requires-python = ">=3.10"
license = { text = "AGPL-3.0-only" }

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
